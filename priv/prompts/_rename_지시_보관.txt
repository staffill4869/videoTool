원래 마지막에 붙어 있던 지시:

  Rename the files by adding sequential numbers such as 01, 02, 03, and so on at the
  beginning of each filename, following the exact order of the script you have worked on
  so far. Keep the file order consistent with the script sequence.

이 단계는 더 이상 필요 없다. videoTool 의 ingest 가 파일명을 무시하고 화면 내용으로 순서를 맞춘다
(CLEAN 은 순서, INFO 는 CLEAN 과 phash 대조, 클립은 첫 프레임=CLEAN·끝 프레임=INFO 체인).
Flow 파일명이 내용과 무관하다는 것이 애초에 이 기능을 만든 이유다.

혹시 수동으로 돌아갈 일이 있으면 위 지시를 그대로 쓰면 된다.