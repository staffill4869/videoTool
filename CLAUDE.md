# videoTool — 이 프로젝트에서 일하는 규칙

이 파일은 세션마다 자동으로 읽힌다. **읽고 시작한다.**
여기 적힌 것은 대부분 실제로 한 번씩 당한 것들이다. 추측이 아니라 실측이다.

---

## 1. 이건 무슨 물건인가

**영상 제작 파이프라인.** CRM 이 아니다 (이름은 옛 흔적이다 — 내부 모듈은 `VideoTool`).

```
주제 → 대본 → 장면 분할 → CLEAN(글자 없는 이미지) → INFO(인포그래픽 얹기)
     → VIDEO(두 이미지 사이를 영상으로) → 나레이션 → 합성 → 발행
```

- 서버: Phoenix, 포트 **4300**
- MCP: `http://127.0.0.1:4300/mcp` — 도구 40여 개
- REST: 모든 MCP 도구를 `POST /api/tools/<이름>` 으로도 부를 수 있다

---

## 2. 역할 경계 — 누가 무엇을 하나

| | 누가 |
|---|---|
| 대본 · 장면 분할 · 허용 수치 | **에이전트만** (말을 만드는 일. 서버엔 LLM 이 없다) |
| Flow 조종 · 수확 · 장면 배정 · 합성 | **서버** |
| 구글 로그인 · 유튜브 OAuth 동의 | **사람만** (자격증명은 자동화가 만지지 않는다) |

**에이전트가 하지 말 것**
- `flow_generate`, `assemble` 을 직접 부르지 마라 — 서버가 `auto_advance` 로 민다. 둘이 동시에 밀면 같은 Chrome 을 서로 뺏는다
- **화면 제어(마우스·키보드)를 쓰지 마라.** MCP 도구만 쓴다. 화면 제어로 떨어졌다면 MCP 가 안 붙은 것이니 그걸 고쳐라
- 사람에게 Flow 탭을 열어달라고 하지 마라 — `flow_new_project(project_id)` 로 직접 연다

**중간에 묻지 마라.** 오류로 막힐 때만 멈춘다. 크레딧 승인도 묻지 마라 — 드라이버가 '항상 승인'을 자동으로 누른다.
유일한 예외는 **유튜브 OAuth** 다. `connected: false` 면 거기서 멈추고 사람을 부른다.

---

## 2.5 파이프라인을 돌릴 때 — 감시는 선택이 아니다 (필수)

**단계를 시작했으면 감시를 건다.** 던져놓고 사용자가 물을 때만 보면 안 된다.
실패가 조용히 온다 — 작업이 `done` 인데 자산 0개, 프롬프트는 맞는데 그림이 딴것,
예약은 돌았는데 로그도 없이 죽음. 전부 실제로 겪은 것이다.

**거는 법**
- `Monitor` 로 **60초 이상** 주기, 한 번에 **쿼리 하나**, **바뀔 때만** 출력
- 15~20초 주기로 psql·curl 을 여러 개 띄우지 마라 — 메모리를 말려 **서버를 두 번 죽였다**
  (실측: 여유 0.4GB / 744 프로세스, MSYS2 `fork` 실패)
- 에이전트 로그를 `tail -F` 로 잡지 마라 — 파일이 잠겨 **무인 루프가 통째로 죽었다**
- 단계가 바뀌면 사용자에게 한 줄로 보고한다. 조용히 있으면 멈춘 것과 구별이 안 된다

**단계마다 확인할 것**
| 단계 | 확인 |
|---|---|
| CLEAN | **이미지를 눈으로 본다.** 내용이 대본과 맞는지, 장면 배정이 맞는지 (여기가 유일하게 사람이 봐야 하는 곳) |
| INFO · VIDEO | 회수 개수와 장면 수. 해시 대조라 배정은 자동으로 맞는다 |
| 나레이션 | 총 길이 == 클립 합계인지 |
| 합성 | 해상도·길이·자막 시작 시각이 장면 경계와 맞는지 |

**배정 신뢰도가 높아도 안심하지 마라.** 0.99 는 "클립이 자기 이미지와 맞는다" 는 뜻이지
"그림이 대본과 맞는다" 는 뜻이 아니다.

---

## 3. Flow 함정 (전부 실제로 당한 것)

### 3.1 주소와 화면 분류
- 도메인이 `labs.google/fx/...` → **`flow.google.com`** 으로 바뀌었다
- **`/about` 과 홈(`/?pli=1`)은 편집기가 아니다.** 편집기는 `/project/<id>` 뿐. 이걸 구별 안 해서 "로그인은 됐는데 입력칸이 없다"로 오진했다
- 결과 이미지 주소가 `flow-content.google` → **`flow.google.com/asb/`** 로 바뀌었다. 옛 호스트만 찾다가 **이미지 17장이 있는데 0장으로 셌다**. 호스트가 아니라 `/asb/` 경로로 잡는다

### 3.2 셀렉터
- 프롬프트 입력칸은 `textarea` 가 아니라 **`contenteditable`**. 페이지의 유일한 `textarea` 는 reCAPTCHA 숨은 필드다
- 새 프로젝트 버튼은 한국어 UI 에서도 **`Get started`**
- **같은 이름을 두 후보 목록에 넣지 마라.** `dismiss` 에 `Get started` 를 넣었더니 덮개 치우는 함수가 새 프로젝트 버튼을 먼저 눌러버려 정작 `newProject` 가 못 찾았다
- `설정 트리거` 는 `hidden` 속성이 붙은 숨은 앵커다. 실제로 눌리는 건 `에이전트 요청 사항`(article_spark)

### 3.3 화면비 — 주문한 대로 나온다 (2026-09-11 정정)
전에 여기 "Flow 가 9:16 을 무시한다"고 적혀 있었다. **틀렸다.** 근거였던 가로 이미지
(1376x768 / 1280x720)는 전부 프로젝트 13 것인데, 그 프로젝트의 `aspect` 가 `16:9` 였다.
Flow 는 주문한 대로 만들었고, 잘못 주문한 건 우리였다.

화면비는 이 사슬로 흐른다 — 한 군데라도 16:9 면 가로가 나온다:

    series.aspect → spawn_project → project.aspect → 프롬프트 장면마다 + Flow 상시 지시

- **그림체 프리셋 13개의 `default_aspect` 가 전부 `16:9` 다.** `create_project` 에서
  `aspect` 를 생략하면 그림체 기본값을 물려받아 가로가 된다 → **세로면 명시해서 넘겨라**
- 시리즈에서 만든 프로젝트는 `series.aspect` 를 물려받는다. 시리즈를 고쳐도
  **이미 만들어진 프로젝트는 안 바뀐다** (13번이 그래서 혼자 16:9 로 남았다)
- 프로젝트를 만든 뒤 `status(project_id)` 로 `aspect` 를 확인하고 시작한다

진짜 9:16 프로젝트를 Flow 에 넣어본 적은 아직 없다. 안 된다고 결론 내지 마라 — 안 해봤다.

### 3.4 대화형이라 멈춘다
- 프롬프트를 넣으면 바로 안 만들고 **구성안을 낸 뒤 확인을 기다린다** → `waitResults` 가 confirm 을 최대 2회 보낸다
- 크레딧을 쓰기 전에 **'승인 / 항상 승인 / 거부'** 를 묻는다 → `approveIfAsked` 가 **'항상 승인'** 을 누른다. '승인'은 한 번뿐이라 다음 단계에서 또 멈춘다
- 우리 장면 구성을 무시하고 다시 쪼갠다 → 프롬프트에 "목록이 곧 장면 개수다"를 박아 잡았다

### 3.5 콘텐츠 필터 — 차단이 아니라 **조용한 대체**로 온다
종이 디오라마 화풍 + 전쟁 + 인물 조합은 위험하다. 한 번은 "미성년자 관련 유해 콘텐츠" 로
**차단**됐고, 2026-09-13 에는 더 나쁜 방식으로 나왔다 — **아무 경고 없이 다른 소재로 바꿔치기**했다.

실측: 장면 지시가 `로마 군단의 붉은 망토 행렬이 초록 언덕을 따라 북으로 오르다 안개 앞에서
멈춘다` 였는데, 나온 그림은 **서부 개척시대 포장마차 행렬**이었다. 프롬프트는 정확히 나갔다
(렌더된 프롬프트에서 원문 확인). 오류도, 거부 메시지도 없었다. 영상·나레이션·자막을 다
만들고 나서야 화면을 열어보고 알았다.

→ **이 화풍에서는 인물·군대를 아예 쓰지 마라.** 사물·지형·구조물로 쓴다:
```
쓰지 마라: "로마 군단의 붉은 망토 행렬이 언덕을 오른다"
이렇게:    "돌포장 길이 언덕을 넘어 북으로 뻗다가 안개 속으로 사라진다"
```
**그리고 CLEAN 이 나오면 이미지를 한 장이라도 눈으로 확인하고 다음 단계로 간다.**
해시 배정 신뢰도가 0.99 여도 그건 "클립이 자기 이미지와 맞는다" 는 뜻이지
"그림이 대본과 맞는다" 는 뜻이 아니다.

### 3.6 탭이 떠내려간다
Flow 탭은 가만히 둬도 편집기에서 홈(`flow.google.com/`)으로 돌아가 있곤 한다. 그러면
`prompt_box: false` 라 자동 조종이 매 tick 수동 폴백으로 빠진다 — 겉보기엔 "조용히 아무것도
안 하는" 상태다. `drive_flow` 는 `status()` 가 아니라 **`ensure_editor(project)`** 를 부른다
(없으면 `open_for` 로 열고 상시 지시까지 다시 넣는다).

**자동화가 할 수 있는 일에서 사람을 부르지 마라.** 사람을 부르는 건 구글 로그인 화면일 때뿐이다.

### 3.7 개수를 셀 때 — 어느 화면에서 세는지부터 정한다
**홈 화면의 프로젝트 썸네일도 생성물과 같은 `/asb/` 주소다.** 실측으로 홈에서 15개가 잡혔다.
그래서 `countResults` 가 편집기 밖에서도 숫자를 냈고, 목표 장수를 넘는 순간 이미지가 한 장도
없는데 "다 만들어졌다" 로 빠져나왔다. 재시도할 때마다 Flow 프로젝트가 하나씩 생겨 썸네일이
늘었으므로 **재시도할수록 더 확실히 실패하는 고리**였다.

→ `countResults` / `waitResults` 는 **URL 이 `/project/` 인 편집기에서만** 센다.
편집기가 아니면 0 을 세는 게 아니라 **오류로 알린다** (조용한 빈손 종료가 제일 찾기 어렵다).

같은 이유로: 후보 셀렉터를 "가장 큰 값" 으로 합치지 마라. 구체적인 것부터 보고
**처음 0 이 아닌 값**을 쓴다. 느슨한 후보를 섞으면 화면 장식까지 세어진다.

### 3.8 Flow 가 물으면 — 읽고 답한다
생성이 끝나면 Flow 는 **"생성이 모두 완료됐습니다. 다음 작업은? (1) 영상 만들기 (2) 장면 수정 …"**
을 묻는다. 드라이버가 보낼 수 있는 말이 `confirmText` 하나뿐이던 시절엔 여기에도
"네, 그대로 진행해 주세요" 를 보냈고, 그건 **1번을 고른 셈**이라 CLEAN 단계에서 영상이 만들어졌다.

- 결과가 **하나도 없을 때만** `confirmText` (구성안 확인용)
- 결과가 나오다 멈추면 `nudgeText` — "모자란 것만 이어서, 영상으로 만들지 말 것"
- **완료 문구가 뜨면 아무 말도 하지 않고 그 단계를 끝낸다**
- 선택지는 글자를 읽고 `stageChoice` 에 적힌 단계에서만 누른다 (`video` 만 해당)

완료 판정에 인사말을 넣지 마라. "무엇을 도와드릴까요" 를 넣었다가 **새 프로젝트의 첫 인사말**에
걸려 이미지 0장으로 끝난 적이 있다. 코드에서도 **결과가 1개 이상 나온 뒤에만** 완료를 인정한다.

### 3.9 CDP 연결
- 포트가 `/json/version` 에 응답하는데 `connectOverCDP` 가 `<ws connected>` 직후 멈추면 → 구글 로그인이 남긴 **cross-origin iframe 타깃**이 범인이다. `connect()` 가 자동으로 치우고 재시도한다
- Chrome 프로필 경로를 **하드코딩하지 마라.** 폴더 이름이 바뀌면 없는 경로로 떠서 로그인 없는 새 프로필이 생긴다 (실제로 프로필이 둘로 갈렸다)

---

## 4. 길이·속도 규칙

- **낭독 속도로 길이를 맞추지 않는다.** 안 맞으면 대본 글자 수를 고친다. 빨리 읽히면 설명이 아니라 광고로 들린다
- `voices.speech_rate` 는 **-0.2~0.1** 로 changeset 이 막고 있다. 이 범위를 넓히지 마라
- **장면 목표와 클립 길이가 1.35배 이상 어긋나면 배속으로 때우지 마라.** 대본을 그 영상에 맞게 고쳐 TTS 를 다시 만든다 — 그게 "말이 빨라지는 구간"의 정체다
- 대본 쓰기 전에 `estimate_length` 로 확인한다. `measured_chars_per_sec` 는 **공백을 뺀** 글자 수 기준이다 (두 곳의 세는 기준이 다르면 추정이 통째로 틀어진다)

---

## 4.5 편집 — 오늘 실측으로 알아낸 것

**길이는 영상이 정한다. 대본이 아니다.**
Flow 클립은 장면 목표와 무관하게 **8초씩** 나온다. 장면 16개면 영상이 120~144초다.
60초로 쓴 대본으로 TTS 를 만들면 뒤 절반이 무음이 된다 (실측: 영상 120초 / 나레이션 67초).

→ 순서를 뒤집는다: **영상 먼저 → 길이 실측 → 그 길이로 대본 → TTS.**
`next_job` 의 `make_narration` 이 `clip_total_sec` 을 실어 준다.

**클립을 잘라 길이를 맞추지 않는다.**
예전 `retime` 은 8초 클립을 장면 목표(3초)로 깎았다 — **만든 영상의 60%를 버렸다.**
기본은 `fit: :clips`(그대로 쓰기)다. 맞추고 싶을 때만 `fit: :scenes`.

**`-shortest` 와 `apad` 는 한 쌍이다.**
- `-shortest` 만 → 나레이션이 짧으면 영상이 거기서 잘린다 (144초 중 84초가 날아갔다)
- `apad` 만 → 오디오가 끝나지 않아 출력이 **영원히 안 끝난다** (9분간 커지기만 했다)
- 함께 → 영상은 전부, 남는 구간은 무음

**해상도를 박아두지 마라.** `enc/1` 이 1920x1080 고정이라 720x1280 세로 클립이
가로 판에 끼워져 좌우가 검게 나왔다. 파일 이름도 `final_16x9.mp4` 로 굳어 있었다.

**자막**
- `WrapStyle: 2` 는 **자동 줄바꿈을 끄는 값**이다. 긴 문장이 화면 밖으로 뻗는다 → `0`
- ASS 의 `PlayResX/Y` 를 1920x1080 으로 고정하면 세로 영상에서 글자가 안 맞는다
  → 9:16 은 `1080x1920`, 52pt, 좌우 여백 80, 아래 260

**클립에도 소리가 있다** (실측 mean -22.5dB). `-map 1:a:0` 로 나레이션만 가져가면
Flow 가 만든 배경음을 통째로 버린다 → `amix` 로 클립 18% + 나레이션 100%.

---

## 4.6 정렬 — 장면이 기준 단위다 (2026-09-13 해결)

**통짜 TTS 를 클립 묶음에 얹는 방식은 폐기했다.** 원리상 안 맞는다:
영상은 클립 누적(0-8, 8-16, 16-24…)으로 가는데 자막·나레이션은 글자 수 비례로
나뉘어(0-6.0, 6.0-11.1…) 네 번째 장면에서 9초까지 벌어졌다.

→ **장면마다 따로 만든다:**
```
장면 N 대사 → TTS → 그 장면 클립 길이(8초)에 맞춰 무음 패딩 → 이어 붙이기
```
나레이션 총 길이가 영상과 **정확히** 같아진다 (실측 120.000초).
`Assembly.clip_timing/1` 이 장면 시간을 클립 실측 길이에서 뽑는다.

**장면 수는 8개가 적당하다.** 클립이 8초씩 나오니 8장면=64초.
16장면은 하루 종일 한 번도 못 채웠는데 8장면은 CLEAN 이 4분에 끝났다.

**TTS 가 다 막혔을 때**: `System.Speech` + `Microsoft Heami`(ko-KR) 가 로컬에 있다.
약 5.2자/초(공백 제외)로 상용(8.2)보다 느리니 대사를 그만큼 짧게 쓴다.

---

## 5. 기타 함정

- **허용 수치를 반드시 채운다.** 안 채우면 INFO 프롬프트가 "허용 수치: (없음) … 목록에 없는 숫자는 절대 넣지 말라" 를 낸 뒤 장면별 지시에서 숫자를 요구하는 **자기모순**이 된다
- **INFO 단계에서 화면 아래 20% 에 글자를 넣지 않는다.** 나중에 하드번한 자막과 겹친다
- **Flow 그리드의 DOM 순서는 장면 순서가 아니다** (1→3, 2→1, 3→5 로 뒤섞인 적 있다). 배정은 첫/끝 프레임 dHash 로 한다
- 서버가 죽으면 `running` 인 Flow 작업 행이 남아 자동 전진이 **영구히 멈춘다** → 부팅 때 자동으로 정리한다
- `POST /api/...` 응답은 `{ok, summary:{...}}` 처럼 한 겹 감싸져 있다. MCP 응답과 모양이 다르다
- 긴 프롬프트를 `System.cmd` **인자로 넘기지 마라.** Windows 인용 처리에서 큰따옴표가 깨진다 → 임시 파일에 쓰고 `--file 경로`
- 한글이 든 `.ps1` 은 **UTF-8 BOM** 으로 저장한다. PowerShell 5.1 이 BOM 없으면 ANSI 로 읽어 파서가 죽는다
- `auto_advance` 를 켜면 `next_advanceable` 이 **id 오름차순**으로 집는다. 묵은 `scened` 프로젝트가 남아 있으면 새로 만든 편이 계속 뒤로 밀린다
- `psql -c` 쿼리 본문에 **한글을 넣지 마라** (bash→psql 이 CP949 로 넘겨 인코딩 오류). SQL 은 ASCII, 한글은 셸 쪽에서 붙인다
- `Start-Process -RedirectStandardOutput` 은 `Kill()` 당하면 **0바이트 파일**을 남긴다. 타임아웃으로 죽은 실행의 출력은 없다고 봐야 한다
- **단계 완료를 화면 숫자로 판정하지 마라.** Flow 가 "대기열에 등록" 만 한 상태에서 125초 만에 done 이 되고 클립 0개로 끝난 적이 있다 → `run_stage` 가 **회수한 개수**로 검증하고 모자라면 최대 4회까지 더 기다린다
- **부수적인 단계가 본 작업을 죽이게 두지 마라.** 두 번 당했다: (1) 로그 파일 잠금이 `$ErrorActionPreference="Stop"` 과 만나 무인 루프 전체를 죽임 (2) 상시 지시 버튼 이름 변경이 CLEAN 시작 자체를 막음
- **진행 감시가 대상을 망가뜨릴 수 있다.** 15~20초 주기 psql/curl 이 메모리를 말려 서버를 두 번 죽였고, `tail -F` 가 로그를 잠가 에이전트를 죽였다. 주기는 60초 이상, 한 번에 쿼리 하나
- 외부 프로그램 출력을 MCP 응답에 실을 때 **UTF-8 검증**을 한다. PowerShell 출력이 CP949 라 `Jason.EncodeError` 로 응답 전체가 못 나간 적이 있다

---

## 6. 다른 컴퓨터에 옮길 때

**필요한 것**
- Elixir/Erlang, PostgreSQL, Node, ffmpeg, Chrome
- `.env` (저장소에 없다 — 자격증명)
- `projects/` 는 생성물이라 `.gitignore` 에 있다. 옮기지 않아도 된다

**순서**
1. `mix deps.get && mix ecto.create && mix ecto.migrate`
2. `mix run priv/repo/seeds.exs` → `sync_prompts.exs` → `sync_voices.exs`
3. `.\launch-chrome.ps1` → **그 창에서 사람이 구글 로그인**
4. `.\restart.ps1`
5. MCP 등록: `http://127.0.0.1:4300/mcp` — **`localhost` 를 쓰지 마라.** `::1`(IPv6)로 풀려 연결이 안 된다. 실제로 30초 타임아웃으로 막혔다
6. 무인 루프를 쓸 거면 `run-agent.ps1` 을 예약 작업에 등록하고, `.claude/settings.local.json` 의 `permissions.allow` 를 확인한다 (비대화형은 승인창을 못 띄워 전부 거부된다)

**UI 가 바뀌어 깨지면** 코드가 아니라 `priv/flow_driver/selectors.json` 을 고친다. 호출할 때마다 다시 읽으므로 서버 재시작이 필요 없다.

---

## 7. 기록 규칙

버그를 고치면 `Obsidian Vault/rebase/videoCRM/교훈.md` 에 한 줄로 남긴다 — `**[스택]** 증상 → 원인 → 규칙`.
커밋과 교훈은 한 세트다. 몰아서 적으면 "왜 그랬는지"가 흐려진다.
