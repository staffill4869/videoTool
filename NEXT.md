# 다음 세션에서 바로 할 일

## 상황 (2026-09-13 01:50 기준)

**프로젝트 21 "역사 — 왜 그렇게 됐나 #7" 은 완성돼 있다.**
영상·자막·정렬 전부 맞는다. 딱 하나, **나레이션 음성이 Windows 내장(Heami)** 이라 품질이 낮다.

이번 세션에서는 힉스필드 MCP 가 `not connected` 였다 (`claude mcp list` 로는 Connected 인데
세션 시작 시점에 연결이 죽어 세션 내내 못 썼다). 구글 TTS 는 403(API 미사용 설정).

```
projects/21/final.mp4   720x1280 · 56.0초 · 클립 7개 · 자막 하드번
http://localhost:4300/renders/14/play
```

## 할 일 — 음성만 교체 (약 3분)

영상은 다시 만들지 않는다. `work/tts/` 의 wav 7개만 갈아끼우고 다시 합성한다.

1. **힉스필드 MCP 로 장면별 음성 7개를 만든다.** 대사는 `lines21.json` 에 있다
   (키 = scene_no: 1,2,3,4,5,6,8). 장면마다 **8초를 넘기지 않아야 한다.**
   낭독 속도로 길이를 맞추지 말 것 — 길면 대사를 줄인다.

2. 받은 파일을 `projects/21/work/tts/sNN.wav` 로 저장한다 (s01, s02, s03, s04, s05, s06, s08).

3. 패딩·이어붙이기·등록은 스크립트가 한다:
   ```powershell
   # make-narration.ps1 은 Windows TTS 로 새로 만드므로 그대로 쓰면 안 된다.
   # 힉스필드 wav 를 이미 넣었다면 2~5단계만 수행하면 된다:
   #   각 wav 를 해당 클립 길이(8초)에 맞춰 apad + -t 로 자르고
   #   concat 으로 이어 붙인 뒤 save_narration 에 넘긴다
   ```
   또는 `make-narration.ps1` 을 고쳐 `-SkipTts` 옵션을 추가해 이미 있는 wav 를 쓰게 한다.

4. `POST /api/tools/assemble {"project_id":21}` 로 다시 합성.

5. 확인: 해상도 720x1280, 길이 56.0초, 자막 시작이 0/8/16/24/32...

## 반드시 지킬 것

- **CLEAN 이 나오면 이미지를 눈으로 본다.** 이번에 내용은 맞는데 장면 번호가 뒤섞여
  (슬롯1←장면2, 슬롯3←장면1 …) 손으로 배정을 고쳐야 했다. 안 봤으면 대사와 화면이 어긋난 채
  끝까지 갔을 것이다. 배정 신뢰도 0.99 는 "클립이 자기 이미지와 맞는다" 는 뜻일 뿐이다.
- **감시를 건다.** 60초 이상 주기, 쿼리 하나, 바뀔 때만. (자세한 것은 CLAUDE.md §2.5)
- 종이 디오라마 화풍에서 **인물·군대를 쓰지 마라.** 필터가 차단이 아니라
  **조용히 다른 소재로 바꿔치기** 한다 (로마 군단 → 서부 개척시대 포장마차).

## 남은 숙제

- [ ] 유튜브 OAuth 재연결 (`login_channel yt-main`) — 토큰이 하루 만에 끊겼다.
      동의 화면을 **"프로덕션" 으로 게시**해야 7일 만료가 사라진다
- [ ] 구글 Cloud TTS 사용 설정 (프로젝트 1046746259031) — 켜면 서버가 직접 TTS 가능
- [ ] INFO 글자가 큰 문제: `priv/prompts/info.txt` 에 "배지·태그 형태, 가로폭 1/3 이내" 를
      넣어뒀다. `mix run priv/repo/sync_prompts.exs` 로 DB 에 반영해야 적용된다
