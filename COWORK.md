# 코워크 예약에 넣을 프롬프트

`/schedule` 로 만든 반복 작업에 **아래 블록을 그대로** 붙인다.
코워크가 매 회차 이걸 읽고 한 편을 끝까지 민다.

수정하면 이 파일도 같이 고친다 — 코워크 UI 안에만 있으면 다음에 찾지 못한다.

---

```
videoTool 로 영상 한 편을 끝까지 만들어 올린다.
서버: http://localhost:4300 · MCP: videocrm

## 권한 — 전권 위임

**중간에 나에게 묻지 마라.** 이 작업은 내가 자는 동안 도는 무인 작업이다.
확인·승인·선택을 요청하면 그 회차는 그냥 멈춘 것과 같다.

- "이렇게 할까요?" "진행할까요?" "어느 쪽으로 할까요?" — 전부 금지. 네가 정해서 간다
- 판단이 갈리면 **더 안전한 쪽**을 골라 그대로 진행하고, 끝난 뒤 보고에만 한 줄 적는다
- 발행은 그냥 올린다. **공개 여부는 네가 정하지 않는다** — 채널 설정이 정하고
  코드가 강제한다. `privacy` 를 넣든 말든 결과는 같으니 신경 쓰지 말고 진행한다
- 사람에게 알릴 일이 생겨도 **거기서 멈추지 말고** 보고에 적어 두고 계속한다

## 막혀도 멈추지 마라

**막힌 것은 회차를 끝낼 이유가 아니다.** 한 곳이 막히면 그 일만 놓고 다른 일로 옮긴다.
"사람 확인이 필요하다" 고 적고 멈추면, 막힌 일 하나 때문에 할 수 있었던 나머지 전부를
안 한 것이 된다. 실제로 그렇게 한 회차가 통째로 날아간 적이 있다.

막혔을 때 옮겨 갈 곳은 늘 있다:

| 막힌 곳 | 하지 말 것 | 대신 할 것 |
|---|---|---|
| Flow 가 로그인 화면 | 회차 종료 | Flow 를 안 쓰는 일로 간다 — 대본·장면·나레이션·편집·섬네일 |
| 한 단계가 두 번 실패 | 세 번째 시도 | 있는 것으로 다음 단계. 모자라면 그 프로젝트를 놓고 **다음 프로젝트** |
| 채널 토큰이 끊김 | 회차 종료 | 업로드만 건너뛰고 다음 프로젝트를 끝까지 만든다 |
| 크레딧 부족 | 되묻기 | 생성이 필요 없는 일(편집·섬네일·발행)로 간다 |
| 무엇을 할지 모르겠음 | 질문 | list_projects 로 가장 많이 진행된 미완료 건을 집는다 |

정말로 할 일이 하나도 안 남았을 때만 회차를 끝낸다. 그때도 **왜 남은 게 없는지**를 적는다.

사람만 할 수 있는 일(구글 로그인)은 시도하지 말고 **보고 맨 위에 굵게 한 줄** 적는다.
적고 나서 하던 일을 계속한다 — 알리는 것과 멈추는 것은 다른 일이다.

## 겹침 방지

시작하자마자 list_projects 와 flow_status 를 본다.
다른 프로젝트에 Flow 작업이 돌고 있으면 **Flow 단계는 건너뛰고**
대본·나레이션·합성·섬네일 같은 Flow 를 안 쓰는 일만 한다. Chrome 은 하나뿐이다.

## 순서

1. list_projects(unfinished_only: true) 로 이어서 할 편을 하나 고른다.
   가장 많이 진행된 것부터 끝낸다 — 새로 벌이지 말고 밀린 것을 닫는다.
   전부 끝났으면 run_series 로 새 편을 만든다.
2. next_job 을 반복해 대본·장면·허용수치를 채운다. 더 안 내줄 때까지.
3. flow_new_project → flow_generate(clean) → flow_job → flow_harvest
4. contact_sheet(clean) 을 눈으로 보고 순서가 틀렸으면 remap_scenes 로 고친다.
   **건너뛰지 마라.** 여기가 틀리면 대사와 화면이 끝까지 어긋난다.
5. 같은 방식으로 info, video. 한 단계를 세 번 이상 돌리지 마라 — 크레딧만 나간다.
6. 힉스필드 TTS → save_narration(scene_secs 포함) → assemble
7. **오타는 합성 전에 훑는다.** 자막은 하드번이라 구운 뒤에는 재합성 말고는 방법이 없다.
   자막에 나가는 건 대본(raw_text)이 아니라 **장면의 segment_text** 다 — 둘이 어긋날 수 있으니
   장면 쪽을 본다. 실제로 대본엔 "젖을" 인데 장면엔 "젫을" 이 들어가 그대로 구워졌다.
   - 의문문에 물음표가 있는가 (없으면 자막 두 줄이 한 줄로 붙는다)
   - 살아남다·빠져나가다처럼 한 단어를 띄우지 않았는가
   - 없는 글자가 섞이지 않았는가 (젫·붸 같은 것)
8. **섬네일**: thumbnail_brief(project_id) 로 지시문을 받아 **그대로** 그린 뒤
   **반드시 save_thumbnail(project_id, file)** 로 붙인다. 파일만 폴더에 두고 끝내지 마라 —
   그래서 "섬네일 없음" 으로 올라간 적이 있다.
   요약해서 그리지도 마라 — 세로로 만들거나 회차 번호를 박은 적이 있다.
   가로 1280x720, 좌우 비교, 글자 덩어리 셋 이하.
9. save_publish_meta → publish(confirm: true).
10. **끝나면 바로 1번으로 돌아간다.** 회차 시간이 남아 있으면 다음 편을 계속 만든다.

## 보고

끝나면 푸시 알림으로 5줄. 무엇을 만들었나 · 몇 장면 · 유튜브 주소 ·
건너뛴 단계와 이유 · 다음 회차가 이어서 할 것.
```

---

## 자주 걸리는 곳

**코워크가 자꾸 확인을 요청한다** — 위 "권한" 블록이 프롬프트 맨 앞에 있어야 한다.
뒤쪽에 두면 긴 작업 중간에 잊는다.

**Flow 가 로그인 화면이다** — 사람이 직접 들어가야 한다. `launch-chrome.ps1` 로 뜬 창에서.

**서버에 새 도구가 안 보인다** — `restart.ps1` 로 서버를 다시 띄워야 MCP 목록이 갱신된다.
